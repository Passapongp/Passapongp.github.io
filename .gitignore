# ---------------------------------------------------------------
# ชุดข้อสอบที่ไม่ใช่ของเราเอง ห้ามขึ้น GitHub
# ไฟล์นี้มาจากการแปลงชุด mock ของผู้อื่น ใช้อ่านเองได้ แต่เผยแพร่ไม่ได้
# เว็บถูกออกแบบให้ทำงานได้ตามปกติเมื่อไม่มีไฟล์นี้
# ---------------------------------------------------------------
data/questions-mock.js
data/questions-mock.json

# ต้นฉบับ PDF และไฟล์ระหว่างทางของการแปลง
Source/
*.pdf
*.txt

# ขยะของระบบปฏิบัติการและเอดิเตอร์
Thumbs.db
desktop.ini
.DS_Store
.vscode/
